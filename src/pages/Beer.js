import React from 'react';
import { Container, Typography } from '@material-ui/core';

const Beer = () => {
	return (
		<Container>
			<Typography variant="h2">Beer</Typography>
		</Container>
	);
};

export default Beer;
